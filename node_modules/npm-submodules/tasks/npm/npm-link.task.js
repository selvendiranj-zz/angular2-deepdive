"use strict";
const path = require("path");
const execa = require('execa');
function npmLink({ cwd, yarn, module = '' }) {
    // const npm = yarn ? 'yarn' : 'npm';
    return execa.shell(['npm', 'link', module].join(' '), {
        cwd: path.resolve(cwd)
    });
}
exports.npmLink = npmLink;
